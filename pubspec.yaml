name: recipe_recive
description: RecipeRecive — digital recipe book for home cooking.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6

  # ── Uncomment when you're ready to add Firebase ──────────────────────
  # firebase_core: ^2.24.0
  # firebase_auth: ^4.15.0
  # cloud_firestore: ^4.14.0
  # firebase_storage: ^11.5.6
  # cached_network_image: ^3.3.1   # for network recipe images

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true

  # ── Uncomment and add your assets when ready ─────────────────────────
  # assets:
  #   - assets/images/

  # ── Uncomment if you add custom fonts ────────────────────────────────
  # fonts:
  #   - family: Roboto
  #     fonts:
  #       - asset: fonts/Roboto-Regular.ttf